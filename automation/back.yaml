nameOverride: "rm-tabou2-back"
image:
  repository: glregistry.boost.open.global/rennes-metropole/tabou2/tabou2/tabou2
  internalPort: 8080
  exposedPort: 8080

resources:
  limits:
    cpu: 300m
    memory: 1Gi

livenessProbe:
  enabled: false
readinessProbe:
  enabled: false

ingress:
  enabled: false
  hosts:
    host: "{{ .Release.Name }}.rennes-metropole-georchestra.karbon.open.global"
